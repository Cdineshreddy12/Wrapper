/**
 * App.tsx is no longer used as the routing entry point.
 * All providers and routing are now configured in main.tsx + routes/router.tsx
 * using @tanstack/react-router.
 *
 * This file is kept as a placeholder to avoid breaking any stale imports.
 */
export default function App() {
  return null
}
